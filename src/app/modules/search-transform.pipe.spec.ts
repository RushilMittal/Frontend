import { SearchTransformPipe } from './search-transform.pipe';

describe('SearchTransformPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchTransformPipe();
    expect(pipe).toBeTruthy();
  });
});
